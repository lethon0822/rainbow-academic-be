<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rainbowuniv.academicmenagmentbe.graduation.GraduationMapper">

    <select id="getMyCompletedCredits">
        SELECT c.type, SUM(c.credit) AS totalCredits
        FROM enrollment e
        JOIN course c
        ON e.course_id = c.course_id
        WHERE e.user_id = #{userId}
        AND e.status = '수강완료'
        GROUP BY c.type;
    </select>

</mapper>