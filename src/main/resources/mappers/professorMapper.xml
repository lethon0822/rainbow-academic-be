<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rainbowuniv.academicmenagmentbe.mapper.ProfessorMapper">

    <!-- 1. 출결 존재 여부 확인 -->
    <select id="checkExistingAttendance" resultType="int">
        SELECT COUNT(*)
        FROM attendance
        WHERE attend_date = #{attendDate}
        AND enrollment_id = #{enrollmentId}
    </select>

    <!-- 2. 출결 등록 -->
    <insert id="insertAttendance">
        INSERT INTO attendance (attend_date, enrollment_id, status, note)
        VALUES (#{attendDate}, #{enrollmentId}, #{status}, #{note})
    </insert>

    <!-- 3. 출결 수정 -->
    <update id="updateAttendance">
        UPDATE attendance
        SET status = #{status}, note = #{note}
        WHERE attend_date = #{attendDate}
        AND enrollment_id = #{enrollmentId}
    </update>


</mapper>

