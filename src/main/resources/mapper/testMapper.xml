<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rainbowuniv.academicmenagmentbe.mapper.TestMapper">


    <!-- 출결 등록 -->
    <insert id="checkAttendance" parameterType="com.rainbowuniv.academicmenagmentbe.dto.AttendanceDto">
        INSERT INTO attendance (attend_date, user_id, status, note)
        VALUES (#{attendDate}, #{userId}, #{status}, #{note})
    </insert>

    <!-- 성적 수정 -->
    <update id="updateMidFinGrades" parameterType="com.rainbowuniv.academicmenagmentbe.dto.GradeDto" >
        UPDATE enrollment
        SET mid_score = #{midScore},
            fin_score = #{finScore}
        WHERE user_id = #{userId}
    </update>



</mapper>